my_list = [16, 38, -1, 7, True, -8]

# Якщо список буде містити більше одного елементу,
# то виконаємо наступну операцію
if len(my_list) > 1:
    my_list = [my_list[-1]] + my_list[:-1]  # Останній елемент списку переносимо на початок

print(my_list)

"""Якщо ж список буде порожнім чи буде містити один елемент, то він залишиться 
таким же"""